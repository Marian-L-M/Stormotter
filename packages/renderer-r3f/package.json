{
  "name": "@otter/renderer-r3f",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Three.js / R3F renderer — not implemented yet.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@otter/renderer-api": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
